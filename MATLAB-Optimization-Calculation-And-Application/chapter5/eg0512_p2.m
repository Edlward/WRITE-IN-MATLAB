%目标函数所对应的设计变量的系数
c=[1;3;1;1;3]
%不等式约束
A=[ 0  0  0 -1 -1;
    0 -1 -2  0 -1;
   -4 -2 -1 -2  0];
b=[-20;-40;-80];
%设计变量的边界约束
lb=zeros(1,5);
%所有变量均为整数变量，故将所有序号组成向量M
M=[1;2;3;4;5];
%判定为整数的误差限
Tol=1e-8;
%求最优解x和目标函数值fval，并返回状态指示
[x,fval]=intprog(c,A,b,[],[],lb,[],M,Tol)
%求出此时所用钢管的数目
s=sum(x)
